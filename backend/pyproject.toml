[project]
name = "meet-and-seat-backend"
version = "1.0.0"
requires-python = ">=3.13"

[tool.bandit]
exclude_dirs = ["tests"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
